<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:colorBackground">@color/background_material_light</item>

        <item name="android:actionButtonStyle">@style/myMenuButtonStyle</item>
        <item name="actionButtonStyle">@style/myMenuButtonStyle</item>
    </style>

    <style name="NightTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/darkPrim</item>
        <item name="colorPrimaryDark">@color/darkPrimDark</item>
        <item name="colorAccent">@color/darkAccent</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:layout">@color/darkBlue</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>


    <style name="myMenuButtonStyle" parent="Widget.AppCompat.ActionBar">
        <item name="android:minWidth">30dp</item>
        <item name="android:maxWidth">45dp</item>
        <item name="android:width">45dp</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="bacCalculatorStyle">
        <item name="android:layout_marginTop">15dp</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_centerHorizontal">true</item>
    </style>

</resources>
